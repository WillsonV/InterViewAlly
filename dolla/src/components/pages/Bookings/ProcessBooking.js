  /*  const ProcessBooking = (db,EventName,displayName,StartTime,EndTime,Organiser) => {
       // event.preventDefault();
        db.collection("Bookings").doc(BookingId).collection("Bookings").add({
          BookingStatus: 'Requested',
          EventName:EventName ,
          UserName: user.displayName,
          StartTime:StartTime,
          EndTime:EndTime,
          Organiser:Organiser
        });
        //setComment("");
      };

      export default ProcessBooking;
*/